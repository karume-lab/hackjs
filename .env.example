# ==========================================
# Root Environment Variables Template
# Copy this to .env to use across the workspace
# ==========================================

# --- Database (Turso / SQLite) ---
# Used natively by `packages/db`
DATABASE_URL="file:local.db"
# DATABASE_AUTH_TOKEN="your_turso_auth_token"

# --- Authentication (Better Auth) ---
# Used natively by `apps/web` routing and `packages/auth`
BETTER_AUTH_SECRET="super-secret-key-replace-me" # Generate with `openssl rand -base64 32`
BETTER_AUTH_URL="http://localhost:3000"

# --- Public Client URLs ---
# Used by frontend client packages like `packages/auth/client.ts`
NEXT_PUBLIC_APP_URL="http://localhost:3000"
EXPO_PUBLIC_APP_URL="http://localhost:3000"
